# CI smoke test (post history-rewrite)

Throwaway file added to trigger a pull_request CI cycle and verify that
workflows no longer hit `startup_failure` after the QVAC-19143 history
rewrite + the follow-up SHA-pin fix in commit `65f006ef`.

This file will be deleted as soon as the PR is closed.
