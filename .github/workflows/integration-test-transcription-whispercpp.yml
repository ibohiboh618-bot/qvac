name: Integration Tests (Whispercpp)

on:
  workflow_dispatch:
    inputs:
      prebuild_package:
        description: "NPM package containing prebuilds (e.g. @qvac/transcription-whispercpp@1.0.0)"
        required: true
        type: string
      run_integration_tests:
        description: "Run the regular integration suite"
        required: false
        type: boolean
        default: true
      run_rtf_benchmarks:
        description: "Run RTF performance benchmarks"
        required: false
        type: boolean
        default: true
      benchmark_matrix_json:
        description: "Optional JSON array overriding the per-runner benchmark matrix"
        required: false
        type: string
      workdir:
        description: "Relative path to package directory in monorepo"
        type: string
        required: false
        default: "packages/transcription-whispercpp"
  workflow_call:
    inputs:
      ref:
        description: "ref"
        type: string
      repository:
        type: string
      run_integration_tests:
        description: "Run the regular integration suite"
        type: boolean
        required: false
        default: true
      run_rtf_benchmarks:
        description: "Run RTF performance benchmarks"
        type: boolean
        required: false
        default: true
      benchmark_matrix_json:
        description: "Optional JSON array overriding the per-runner benchmark matrix"
        type: string
        required: false
      workdir:
        description: "Relative path to package directory in monorepo"
        required: false
        default: "packages/transcription-whispercpp"
        type: string

jobs:
  run-integration-tests:
    timeout-minutes: 60
    continue-on-error: true
    runs-on: ${{ matrix.runner || matrix.os }}
    environment: release
    name: ${{ matrix.platform }}-${{ matrix.arch }}-integration-tests

    env:
      GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}

    permissions:
      contents: read
      packages: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            platform: linux
            arch: x64
            runner: qvac-ubuntu2204-x64
            benchmark_matrix_json: >-
              [{"modelFile":"ggml-tiny.bin","useGPU":false,"backendHint":"cpu"}]
          - os: ubuntu-24.04
            platform: linux
            arch: x64
            runner: qvac-ubuntu2404-x64-gpu
            benchmark_matrix_json: >-
              [{"modelFile":"ggml-tiny.bin","useGPU":false,"backendHint":"cpu"},{"modelFile":"ggml-tiny.bin","useGPU":true,"backendHint":"cuda"}]
          - os: ubuntu-24.04-arm
            platform: linux
            arch: arm64
            benchmark_matrix_json: >-
              [{"modelFile":"ggml-tiny.bin","useGPU":false,"backendHint":"cpu"}]
          - os: macos-14-xlarge
            platform: darwin
            arch: arm64
            benchmark_matrix_json: >-
              [{"modelFile":"ggml-tiny.bin","useGPU":false,"backendHint":"cpu"},{"modelFile":"ggml-tiny.bin","useGPU":true,"backendHint":"coreml"}]
          - os: macos-15-large
            platform: darwin
            arch: x64
            benchmark_matrix_json: >-
              [{"modelFile":"ggml-tiny.bin","useGPU":false,"backendHint":"cpu"},{"modelFile":"ggml-tiny.bin","useGPU":true,"backendHint":"coreml"}]
          - os: windows-2025
            platform: win32
            arch: x64
            runner: qvac-win25-x64-gpu
            benchmark_matrix_json: >-
              [{"modelFile":"ggml-tiny.bin","useGPU":false,"backendHint":"cpu"},{"modelFile":"ggml-tiny.bin","useGPU":true,"backendHint":"directml"}]

    steps:
      - name: Manual Workspace Cleanup
        if: runner.environment != 'github-hosted'
        shell: bash
        run: rm -rf "$GITHUB_WORKSPACE" && mkdir -p "$GITHUB_WORKSPACE"

      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          repository: ${{ inputs.repository || github.repository }}
          ref: ${{ inputs.ref || github.ref }}
          token: ${{ secrets.PAT_TOKEN }}

      - name: Bootstrap
        uses: ./.github/actions/run-desktop-integration-tests/bootstrap

      - name: Install NPM dependencies
        working-directory: ${{ inputs.workdir }}
        shell: bash
        run: npm install

      - name: Resolve prebuilds
        uses: ./.github/actions/run-desktop-integration-tests/resolve-prebuilds
        with:
          platform: ${{ matrix.platform }}
          workdir: ${{ inputs.workdir }}
          prebuild-package: ${{ inputs.prebuild_package }}
          rename: double-underscore

      - name: Install gh cli for ubuntu-24.04-arm
        if: matrix.os == 'ubuntu-24.04-arm'
        shell: bash
        run: |
          (type -p wget >/dev/null || (sudo apt update && sudo apt-get install wget -y)) \
            && sudo mkdir -p -m 755 /etc/apt/keyrings \
                  && out=$(mktemp) && wget -nv -O$out https://cli.github.com/packages/githubcli-archive-keyring.gpg \
                  && cat $out | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null \
            && sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg \
            && echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
            && sudo apt update \
            && sudo apt install gh -y
          gh --version
        continue-on-error: true

      - name: Install system dependencies
        uses: ./.github/actions/run-desktop-integration-tests/install-system-deps
        with:
          platform: ${{ matrix.platform }}
          arch: ${{ matrix.arch }}
          os: ${{ matrix.os }}
          # whisper.cpp runtime deps for the integration tests
          linux-arm64-extra-packages: libopenblas-dev liblapack-dev libfftw3-dev
          macos-brew-packages: openblas lapack fftw
          set-windows-vulkan-sdk: "true"

      - name: Cache integration test models
        id: model-cache
        if: startsWith(matrix.runner, 'qvac-')
        uses: ./.github/actions/cache-models
        with:
          package: transcription-whispercpp
          paths: packages/transcription-whispercpp/models
          hash-files-glob: packages/transcription-whispercpp/test/integration/helpers.js

      - name: Run integration test (Unix)
        if: ${{ matrix.platform != 'win32' && inputs.run_integration_tests != false }}
        working-directory: ${{ inputs.workdir }}
        shell: bash
        run: npm run test:integration
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}

      - name: Run integration test (Windows)
        if: ${{ matrix.platform == 'win32' && inputs.run_integration_tests != false }}
        working-directory: ${{ inputs.workdir }}
        shell: powershell
        run: npm run test:integration
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}

      - name: Run Vulkan GPU smoke test
        if: matrix.runner == 'qvac-ubuntu2404-x64-gpu'
        working-directory: ${{ inputs.workdir }}
        shell: bash
        run: npm run test:stand-alone:gpu
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}

      - name: Run RTF benchmark (Unix)
        if: ${{ always() && !cancelled() && matrix.platform != 'win32' && inputs.run_rtf_benchmarks != false }}
        continue-on-error: true
        working-directory: ${{ inputs.workdir }}
        shell: bash
        run: node scripts/run-rtf-benchmark-matrix.js
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
          QVAC_WHISPER_BENCHMARK_DEVICE: ${{ matrix.os }}
          QVAC_WHISPER_BENCHMARK_RUNNER: ${{ matrix.os }}
          QVAC_WHISPER_BENCHMARK_MATRIX_JSON: ${{ inputs.benchmark_matrix_json || matrix.benchmark_matrix_json }}

      - name: Run RTF benchmark (Windows)
        if: ${{ always() && !cancelled() && matrix.platform == 'win32' && inputs.run_rtf_benchmarks != false }}
        continue-on-error: true
        working-directory: ${{ inputs.workdir }}
        shell: powershell
        run: node scripts/run-rtf-benchmark-matrix.js
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
          QVAC_WHISPER_BENCHMARK_DEVICE: ${{ matrix.os }}
          QVAC_WHISPER_BENCHMARK_RUNNER: ${{ matrix.os }}
          QVAC_WHISPER_BENCHMARK_MATRIX_JSON: ${{ inputs.benchmark_matrix_json || matrix.benchmark_matrix_json }}

      - name: Upload RTF results
        if: ${{ always() && !cancelled() && inputs.run_rtf_benchmarks != false }}
        continue-on-error: true
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # 7.0.0
        with:
          name: rtf-results-${{ matrix.os }}-${{ matrix.platform }}-${{ matrix.arch }}
          path: ${{ inputs.workdir }}/benchmarks/results/rtf-benchmark-*.json
          retention-days: 30
          if-no-files-found: ignore

      - name: Add RTF summary
        if: ${{ always() && !cancelled() && inputs.run_rtf_benchmarks != false }}
        continue-on-error: true
        working-directory: ${{ inputs.workdir }}
        shell: bash
        run: |
          echo "### RTF - ${{ matrix.platform }}-${{ matrix.arch }}" >> "$GITHUB_STEP_SUMMARY"
          node -e "
            const fs = require('fs')
            const path = require('path')
            const resultsDir = path.resolve('benchmarks/results')
            if (!fs.existsSync(resultsDir)) process.exit(0)
            const files = fs.readdirSync(resultsDir)
              .filter((file) => file.startsWith('rtf-benchmark-') && file.endsWith('.json'))
              .sort()
            if (files.length === 0) process.exit(0)
            const lines = [
              '| Model | GPU | Backend | Device | Mean RTF | P50 | P95 |',
              '|-------|-----|---------|--------|----------|-----|-----|'
            ]
            for (const file of files) {
              const report = JSON.parse(fs.readFileSync(path.join(resultsDir, file), 'utf8'))
              const model = report.model && report.model.name ? report.model.name.replace(/\.bin$/, '') : 'unknown'
              const summary = report.summary || {}
              const rtf = summary.rtf || {}
              lines.push(
                '| ' + model +
                ' | ' + ((report.requested && report.requested.useGPU) ? 'yes' : 'no') +
                ' | ' + ((report.labels && report.labels.backend) || (report.requested && report.requested.backendHint) || 'n/a') +
                ' | ' + ((report.labels && (report.labels.device || report.labels.runner)) || 'n/a') +
                ' | ' + (rtf.mean !== undefined ? Number(rtf.mean).toFixed(4) : 'n/a') +
                ' | ' + (rtf.p50 !== undefined ? Number(rtf.p50).toFixed(4) : 'n/a') +
                ' | ' + (rtf.p95 !== undefined ? Number(rtf.p95).toFixed(4) : 'n/a') +
                ' |'
              )
            }
            console.log(lines.join('\n'))
          " >> "$GITHUB_STEP_SUMMARY"
